import React from 'react';

/**
 * GlobalYouTubePlayer
 * The actual YouTube IFrame Player is now permanently mounted directly inside RadioPlayerView's 16:9 stage
 * without DOM reparenting/teleportation to ensure video stream continuity, responsive resizing, and no iframe reloads.
 */
export const GlobalYouTubePlayer: React.FC = () => {
  return null;
};
